apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami-shared
  namespace: whoami
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-cluster-issuer
    ingress.cilium.io/loadbalancer-mode: shared
spec:
  ingressClassName: cilium
  tls:
    - hosts:
        - whoami-shared.euclid.stonegarden.dev
      secretName: whoami-shared-tls
  rules:
    - host: whoami-shared.euclid.stonegarden.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: whoami
                port:
                  number: 80
